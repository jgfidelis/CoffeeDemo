export type Venue = {
  image_url: string, 
  photos: string[], 
  name: string, 
  url: string, 
  location: { display_address: string[] }, 
  review_count: number, 
  price: string, 
  rating: number, 
  display_phone: string,
};
